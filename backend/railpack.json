{
  "install": {
    "cmd": "npm install --legacy-peer-deps"
  },
  "build": {
    "cmd": "npx prisma generate --schema=src/prisma/schema.prisma && npm run build"
  },
  "deploy": {
    "cmd": "node dist/main.js"
  }
}
